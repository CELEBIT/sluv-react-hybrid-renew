import styled from '@emotion/styled'

export const FilterListWrapper = styled.div`
  display: flex;
  flex-direction: row;
  padding: 0 1.25rem;
  overflow-x: scroll;
  gap: 0.5rem;

  ::-webkit-scrollbar {
    display: none;
  }
`
